# RAG Strategy
The MVP uses governed lexical retrieval because the policy corpus is tiny. Move to pgvector/vector retrieval when corpus size, semantic misses, metadata filtering or versioned multi-document search justify it.

A vector database is not added merely to make the architecture look more AI.
