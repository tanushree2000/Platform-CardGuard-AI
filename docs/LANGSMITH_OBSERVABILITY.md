# LangSmith / AI Observability Plan
Trace session, prompt/model version, intent/confidence, retrieval, tool calls, approval, escalation, outcome, latency and token/cost metadata.

Dashboards: task success, tool accuracy, groundedness, specialist override, injection blocks, approval violations, p95 latency and cost.
